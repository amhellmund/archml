# Negative: interface references that do not match any defined interface
# Expected errors:
#   component 'OrderService': 'requires UnknownRequest' — refers to unknown interface 'UnknownRequest'
#   component 'LegacyClient': 'requires GhostPayment' — refers to unknown interface 'GhostPayment'

interface OrderRequest {
    order_id: String
    currency: String
}

interface PaymentRequest {
    amount: Float
}

component OrderService {
    requires UnknownRequest
}

component LegacyClient {
    requires GhostPayment
}
