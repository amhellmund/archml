# Negative: duplicate type name at top level
# Expected error: Duplicate type name 'Address'

type Address {
    street: String
    city: String
}

type Address {
    line1: String
    line2: String
    postcode: String
}
